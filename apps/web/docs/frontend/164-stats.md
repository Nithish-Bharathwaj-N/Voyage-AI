# 164 - Statistics & Gamification

The `ProfileStats` component visualizes the `ProfileStats` payload.
In Phase 3, these metrics will be calculated automatically by the backend via event triggers (e.g., completing a trip bumps `tripsCompleted` and adds to `travelScore`).
