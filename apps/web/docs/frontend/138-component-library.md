# 138 - Component Library

### `useTrip(id: string)`
Custom hook wrapping `tripRepository.getTrip()`.

### `TripDetailsLayout`
Accepts `hero`, `tabs`, `content`, and `sidebar` nodes.

### `TripDetailsSkeleton`
Animated pulse state matching the layout.

### `QuickActionsPanel`
Reusable right-side action stack.
