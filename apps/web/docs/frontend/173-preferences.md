# 173 - Travel Preferences & AI

The `TravelPreferencesCard` is critical. It configures the user's base travel styles, budget, and dietary restrictions. 
In future AI sprints, the AI generation engine will query these preferences from the database and inject them into the system prompt (e.g. "Generate an itinerary for Rome, ensuring all restaurants match [Dietary] and accommodation matches [Accommodation]").
