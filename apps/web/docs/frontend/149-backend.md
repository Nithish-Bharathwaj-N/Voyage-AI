# 149 - Backend Integration

In Sprint 9A, the following backend connections will be built:
1. Connecting `DestinationRepository` to `/api/v1/destinations`.
2. Implementing the `useMutation` hook for "Save Destination" (adding it to the user's saved collections).
3. Linking "Add to Trip" to open a modal that selects an active trip and appends the destination via the Planner API.
