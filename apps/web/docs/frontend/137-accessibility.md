# 137 - Accessibility

- **Roles**: Layout components use proper landmark HTML elements where appropriate.
- **Contrast**: Text on top of the Hero image is protected by two distinct gradients: a solid fallback color and a bottom-up black gradient, ensuring text contrast regardless of the image.
- **Focus**: Tab navigation and action buttons have explicit `hover` and `focus` states.
