# 177 - Accessibility

- **Forms**: All inputs (like First Name, Last Name) have associated labels.
- **Toggles**: The custom toggle switches are visually styled but semantically they are `<input type="checkbox" className="sr-only">`. This ensures full screen reader support and keyboard tab navigation.
- **Semantic Tags**: The main navigation is a `<nav>` tag containing `<button>` elements.
