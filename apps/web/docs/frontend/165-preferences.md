# 165 - Travel Preferences

The `TravelPreferencesCard` maps the `TravelPreference` arrays into distinct blocks of pill-shaped tags. This data is critical because future AI planner generations will read these preferences to tailor itineraries (e.g., automatically suggesting vegetarian restaurants if the 'Dietary' preference contains 'Vegetarian').
