# 198 - Accessibility Decisions

- **ARIA Attributes**: `PlannerLoading` features an assertive ARIA live region to announce that the planner is working.
- **Semantic HTML**: The `DailyTimeline` uses `h3` and `h4` semantic structure.
- **Focus Management**: The `PlannerInput` takes focus automatically when the user clicks 'Refine'.
