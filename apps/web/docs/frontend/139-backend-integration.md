# 139 - Backend Integration

In Sprint 8B, the following will be implemented:
1. `useMutation` hooks for the actions in `QuickActionsPanel`.
2. Hitting the actual `/api/v1/trips/:id` endpoint in `TripRepository`.
3. Wiring the 'Edit Itinerary' button into the real Planner route.
