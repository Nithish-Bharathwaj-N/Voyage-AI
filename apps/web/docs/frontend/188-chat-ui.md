# 188 - Chat UI Components

The UI is modeled after premium Copilots (Cursor, Claude).
- `ChatPanel`: A slide-over drawer accessible globally via a FAB (Floating Action Button).
- `ChatSidebar`: Manages history.
- `Messages`: Renders user/assistant bubbles.
- `ChatInput`: Auto-resizing input with suggestion chips.
