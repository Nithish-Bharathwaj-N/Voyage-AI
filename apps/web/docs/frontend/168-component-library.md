# 168 - Component Library

### `ProfileHero`
Complex layout component managing negative margins to overlap the avatar circle cleanly over the bottom of the cover photo.

### `ActivityTimeline`
A vertically connected list utilizing CSS absolute positioning for the connecting line (`border-l-2`) and dynamically colored icons based on the event `type`.
