# 145 - Gallery

The `Gallery` uses a dynamic CSS grid layout where the first image spans 2 columns and 2 rows (on desktop) to act as a feature image, while the rest are standard aspect-square thumbnails.
All images use `next/image` with `object-cover` and a CSS zoom on hover.
