# 148 - Component Library

### `useDestination(id: string)`
Custom hook wrapping `destinationRepository.getDestination()`.

### `DestinationTabs`
A horizontal, scrollable sticky nav using Framer Motion `layoutId` for the active indicator.

### `QuickActionsPanel`
The primary interaction hub allowing users to Save to Collections or Add to Trip.
