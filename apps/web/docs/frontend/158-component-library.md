# 158 - Component Library

### `CollectionsToolbar`
Displays the page header and primary actions (New Collection, Search).

### `CollectionsFilter`
Provides segmented control tabs for filtering and a native `<select>` dropdown for sorting.

### `CollectionsEmptyState`
Context-aware empty state that changes its messaging depending on whether a search query is active or the user truly has zero collections.
