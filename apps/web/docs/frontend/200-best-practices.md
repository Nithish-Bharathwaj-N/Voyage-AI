# 200 - Best Practices

- **Strict Schema Enforcement**: Never allow the LLM to write raw text directly to the planner UI.
- **Zero Any Policy**: All data structures mapped from the AI are strongly typed via `AITripPlan`.
