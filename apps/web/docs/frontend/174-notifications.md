# 174 - Notifications Model

The `NotificationsCard` utilizes native toggle switches (built with CSS peer classes on hidden checkboxes) to manage messaging preferences. These map directly to boolean flags in the database, which will be read by the future push notification/email microservice.
