# 56. Responsive Timeline

On mobile devices (`< sm`), the vertical timeline connector (the dot and the line) is completely hidden (`hidden sm:flex`). 
This reclaims 40px of horizontal space on narrow screens, ensuring the activity cards themselves remain readable without text truncation. The cards span full-width on mobile.
