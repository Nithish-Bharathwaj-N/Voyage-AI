# 47. Accessibility

- Tab indexes are explicitly managed between panels.
- The `PlannerContextPanel` uses semantic `<button>` tags for its tabs rather than `divs`.
- The `PlannerCanvas` uses a semantic `<section>` for each Day, with an `<h2>` heading acting as the sticky section header.
