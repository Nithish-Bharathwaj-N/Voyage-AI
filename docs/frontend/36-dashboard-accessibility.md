# 36. Dashboard Accessibility

- **Focus Management**: The Cmd+K Command Palette traps focus when opened and returns focus when closed via ESC.
- **Color Contrast**: The `TripCard` cover images utilize a `bg-gradient-to-t from-black/60` overlay to ensure the white destination text remains AAA compliant regardless of the underlying image colors.
