# 57. Accessibility

All `BaseCard` components include `focus-within:ring-2 focus-within:ring-primary` styles. This ensures that when a keyboard user tabs into the hidden context menu buttons, the entire card illuminates with a focus ring, providing clear spatial context of their current position in the DOM.
