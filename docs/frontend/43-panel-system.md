# 43. Panel System

The right-hand side of the planner is divided between the `PlannerMapPanel` and the `PlannerContextPanel`.
Currently implemented as a vertical split (50/50), future iterations will likely implement drag handles (via `react-resizable-panels` or similar) to allow users to resize the map vs. the AI Context window.
