# 49. Future Integration

The shells built here are completely dumb and presentation-only. 
In Sprint 5D and 5E, the `PlannerCanvas` will be wrapped in a DnD Kit `<DndContext>`, and the `PlannerMapPanel` will dynamically import a Mapbox GL component to prevent blocking the initial thread.
